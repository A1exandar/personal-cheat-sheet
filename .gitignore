# Hugo build output and cache - regenerated by `hugo`, not source content
public/
resources/
.hugo_build.lock

# OS and editor cruft
.DS_Store
content.save

# Local Claude Code runtime state
.claude/
